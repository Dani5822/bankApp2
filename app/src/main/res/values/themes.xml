<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Navigateside" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->

        <item name="colorPrimary">@color/orange_700</item>
        <item name="colorPrimaryVariant">@color/orange_700</item>
        <item name="colorOnPrimary">@color/white</item>


        <!-- Secondary brand color. -->
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/orange_700</item>

        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.Navigateside.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.Navigateside.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.Navigateside.PopupOverlay" >
        <item name="android:textColorSecondary">@color/black</item>
    </style>

    <style name="backgroundcolor">
        <item name="android:background">@color/gray</item>
    </style>
    <style name="errormessage">
        <item name="android:textColor">@color/red</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">20dp</item>
    </style>
    <style name="edittext">
        <item name="android:textColor">@color/black</item>
        <item name="android:textColorHint">@color/darkgray</item>
    </style>
    <style name="button">
        <item name="android:textColor">@color/white</item>
        <item name="android:background">@color/orange_700</item>
    </style>
    <style name="card">
        <item name="android:background">@color/white</item>
    </style>

    <style name="plus">
        <item name="tint">@color/orange_700</item>
        <item name="borderWidth">0dp</item>
        <item name="backgroundTint">@color/white</item>
    </style>

    <style name="deletebutton">
        <item name="android:textColor">@color/white</item>
        <item name="android:backgroundTint">@color/red</item>
        <item name="android:textSize">15dp</item>
    </style>

    <style name="divider">
        <item name="android:divider">@color/gray</item>
        <item name="android:dividerHeight">10dp</item>
    </style>

    <style name="Theme.Navigateside.costume">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowCloseOnTouchOutside">true</item>
    </style>

</resources>